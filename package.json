{
  "name": "express-typed-backend",
  "version": "1.0.0",
  "description": "This will be an express backend with typescript.",
  "main": "src/server.ts",
  "scripts": {
    "dev": "npx nodemon --exec ts-node src/server.ts",
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Karamjeet Singh",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/express": "^5.0.0",
    "nodemon": "^3.1.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}
